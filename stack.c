#include "monty.h"
/**
 * _stack - prints the top
 * @head: stack head
 * @line_counter: line_number
 * Return: no return
 */
void _stack(stack_t **head, unsigned int line_counter)
{
    (void) head;
    (void) line_counter;
    program.change_ds = 0;
}
